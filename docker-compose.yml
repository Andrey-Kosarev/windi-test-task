services:
  database: 
    image: postgres:17.5
    env_file:
      - .env
  webapp:
    build:
      context: .
      dockerfile: src/entrypoints/webapp/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env